{
    "name": "bilibili-watchlater-sorter",
    "type": "module",
    "private": true,
    "scripts": {
        "build": "npm run cleanup && swc src -d output",
        "cleanup": "rm -rf output"
    },
    "devDependencies": {
        "@antfu/eslint-config": "^2.6.2",
        "@swc/cli": "^0.1.63",
        "@swc/core": "^1.3.102",
        "eslint": "^8.56.0",
        "typescript": "^5.3.3"
    }
}
